/**
 * Detects if the text ends with a potential incomplete setext heading underline
 * and adds a space to break the setext heading pattern
 */
export declare const handleIncompleteSetextHeading: (text: string) => string;
